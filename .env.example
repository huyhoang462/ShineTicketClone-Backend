# Server Configuration
PORT=8080
NODE_ENV=development

# JSON Web Token Secrets
# IMPORTANT: Replace these with your own long, random, secret strings
ACCESS_TOKEN_SECRET=your_super_secret_access_token
REFRESH_TOKEN_SECRET=your_super_secret_refresh_token

# MongoDB Connection
# Replace with your own MongoDB Atlas connection string or local MongoDB URI
# Example: DB_CONNECTION_STRING=mongodb+srv://<username>:<password>@cluster0.mongodb.net/your_db_name
DB_USERNAME=
DB_PASSWORD=
DB_CONNECTION_STRING=

# Nodemailer Configuration (Optional, for email features)
# You can use your own Gmail account with an App Password, or another email service
MAIL_USER=your_email@example.com
MAIL_PASS=your_app_password